<VirtualHost *:80>
    DocumentRoot /var/www/html

    <Directory /var/www/html>
        Options FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/pixelpost-error.log
    CustomLog ${APACHE_LOG_DIR}/pixelpost-access.log combined
</VirtualHost>

